describe("notification.vue", () => {
    afterEach(  () => {} );
    beforeEach( () => {} );
    beforeAll(  () => {} );
    afterAll(   () => {} ); 
    // Happy paths
    it.todo( "can be rendered" );
    it.todo( "can add a new item" );
    it.todo( "can edit an item" );
    it.todo( "can remove an item" );
    it.todo( "can render a list of items" )
    it.todo( "can render `not found` when there are no records" );
    // Unhappy paths
    it.todo( "can show an error when an invalid name is filled and try to save it" );
    it.todo( "can show an error when the item name is empty and try to save it" );
} );   